# Blossom Voice — prefer a dedicated .venv (see setup-venv.ps1).
#
# Why: style-bert-vits2 pins omegaconf==2.0.6, which modern pip (>=24.1) rejects.
# setup-venv.ps1 installs pip<24.1 for that step, then the rest of this file.
#
# Manual (inside Voice\.venv):
#   python -m pip install "pip<24.1"
#   pip install torch==2.6.0+cu124 torchaudio==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124
#   pip install omegaconf==2.0.6
#   pip install -r requirements.txt

fastapi>=0.115.0
uvicorn>=0.30.0
numpy>=1.23.0,<2
scipy>=1.10.0
PyYAML>=6.0
huggingface_hub>=0.20.0
edge-tts>=6.1.0
style-bert-vits2>=2.5.0
rvc-python>=0.1.5
torchcrepe>=0.0.20
# torch / torchaudio: install CUDA builds separately (see setup-venv.ps1)
